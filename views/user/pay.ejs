<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dummy Payment Gateway</title>
    <!-- Add Bootstrap CSS link -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/user/pay.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .payment-form {
            max-width: 400px;
            margin: 50px auto;
            background: #fff;
            padding: 30px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <div class="container-box bg-white">
        <div class="row">
          <div class="col-md-6">
    <div class="image-container">
        <img
          src="/images/payment.gif"
          alt="Online Payment Image"
        />
      </div>
          </div>
          <div class="col-md-6">
          <div class="image-container">
<div class="container">
    <div class="payment-form">
        <h2 class="text-center mb-4">Payment Details</h2>
        <p>Items Orderd : <%= order.order %></p>
        <p>Total Amount : <%= order.total %></p>
                <form action="<%= order.id %>/pay">
            <div class="form-group">
                <label for="paymentMethod">Payment Method</label>
                <select class="form-control" name="mode" id="paymentMethod" required>
                    <option value="ob">Online Banking</option>
                    <option value="cod">Cash on Delivery</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary btn-block"> <a href="" style="color: #f8f9fa;"> Pay</a></button>
        </form>
    </div>
</div>
          </div>
          </div></div>
    </div>
    
<!-- Add Bootstrap JS and Popper.js scripts -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
